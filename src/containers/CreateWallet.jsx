import React from 'react';
import BaseButton from '../components/Buttons/BaseButton';

export default function CreateWallet() {
  return <BaseButton text={'Create a Wallet'} variant="contained" />;
}
